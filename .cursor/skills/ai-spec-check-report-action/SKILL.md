---
name: ai-spec-check-report-action
description: >-
  Obsidian 正本 `DevProject/FishTrack/ai_spec_check_report.md` の運用ルール（§1〜§6）に沿って、
  §4 のオープン対策を **FishTrack `src`/`tests`** で実装し、**同一セッションで §6 記録・§4 消込・§5 整理**まで
  済ませるターンの手順書。**プレビュー実行（virtual／dump／本番ログ）は前提にしない**。
  **§4 消込**: **`src` 反映完了がトリガー**。**再プレビュー未取得でも当該 `対策 N` を §4 から除去**（正本・myrules・`ai-spec-check-report` §8.1）。
  **共通手順の詳細・禁止項・対策文の練り直し**は **`ai-spec-check-report` SKILL §8／§8.1** が正本。
  ai_spec_check_report_action／レポート正本に沿った実装／対策 N のコード反映／§4 消込 の依頼時に使用する。
---

# AI スペック照合レポート正本に沿った実装・レポート更新（ai-spec-check-report-action）

## 正本・関連 SKILL

- **レポート正本**: `D:/OneDrive/アプリ/remotely-save/Obsidian/DevProject/FishTrack/ai_spec_check_report.md`
  - 冒頭 **運用ルール（§1〜§6）** と本文 **`## 1.`〜`## 6.`** を **同一セッションで矛盾なく守る**。
**共通手順・対策案の練り直し・§4 文案ルール**: **`dev-workspace`** の **`ai-spec-check-report/SKILL.md`**（冒頭 **禁止（再発防止）**・**§8**・**§8.1**）。
- **実装ターン専用の禁止・起因・チェックリスト拡張**: 本 SKILL の **禁止（実装ターン・再発防止）**・**§4 メンテの典型原因（実装ターン寄り）**・**報告直前チェックリスト（本 SKILL 用）**。
- **横断ルール**: **`dev-workspace`** **`myrules.mdc`**（FishTrack **`ai_spec_check_report.md`** の **`## 4.`** 節）。

## 発火条件（いつ Read するか）

- ユーザーが **`ai_spec_check_report_action`** と明示する、または **レポート正本に沿って §4 の対策をコードへ反映**すると説明がある。
- **`FishTrack`** の **`src/`・`tests/`** を、正本 **§4** で追跡していた **`対策 N`** の打ち手として変更するとき（**入口 `/ai-spec-check-*` を回していないターンでも発火**）。

## 禁止（実装ターン・再発防止）

**共通 SKILL**（`ai-spec-check-report/SKILL.md`）の冒頭「禁止」と **§8.1** も併読する。実装ターンでは特に以下を厳守する。

- **`FishTrack` の `src`／`tests` を、正本 `ai_spec_check_report.md` §4 で追跡していた **`対策 N` の打ち手として変更した同一セッション**において、次まで**ユーザー返答前に**済ませないこと：\
  **`## 6.` の実装記録**（**未取得の再プレビュー**のみでよいときも **未取得を明記**）、**`## 4.` の当該 `対策 N` 消込**、**`## 5.` の整理**。\
  マルチルートで別ボールトだからといって省略禁止。詳細は下記 **報告直前チェックリスト**（**`## 6.`・正本 Read**の各項）。
- 正本 §4 に **「ソース反映済」「コード反映済」「§6 … に記録」の段落だけを追加**し、**実装済みの `対策 N` の対応表・本文を §4 に残したまま**にすること（**消込の代替にしない**。**完了の宣言は §6 のリスト項目のみ**。**`ai-spec-check-report` §8.1「反映完了を記録するレポート更新」**と整合）。
- **誤認禁止**: **「再プレビューで効果を確認するまで §4 から `対策 N` を消さない」**は**正本・myrules・§8.1 に反する**。**`src`／`tests` への反映完了と同一セッション**で **`## 4.` から削除**（**未取得の再プレビュー**は §6 で明示すれば足りる）。

## §4 メンテの典型原因（実装ターン寄り）

**共通 SKILL §8.1「起因」項 1・2（別ボールト・コード反映済の混同）**も読むこと。実装チャットでは次を特に意識する。

1. **実装だけで終了**：ユーザーが **`@…/ai_spec_check_report.md` の §4 アンカーを示して実行依頼**したターンでも、**`FishTrack` だけ更新して **`## 6.` 追記／§4 消込**を別タスク扱いに残す**。**同一セッション**で **`StrReplace`／`Write` により正本まで完結**する（「報告直前チェックリスト」）。
2. **§8 と §8.1 の読み落とし**：§8 が「対策案を増やせ」だけに見えて、**対応済み削除**である **§8.1 メンテ**が **レポート確定チェックリストに無い**。**markdownlint 通過≠§4 適合**。

## 作業フロー（省略禁止の順序）

1. **正本を Read**  
   - フロントマターの **`resolvedUrl`**・**`checked_at`**・**`previewRunId`**（ある場合）を把握。**編集は運用ルールに反しないか**を確認する。
2. **`ai-spec-check-report` SKILL を Read**  
   - **§8**・**§8.1**・冒頭 **禁止（再発防止）** を読んでから実装に入る。
3. **コード実装**（対象が **`FishTrack`** のとき）  
   - **`specification-update` SKILL** に従い、振る舞い変更に応じて **`DevProject/specifications/FishTrack/`**（典型 **`05_ai_spec_import.md`**）を同期する。**仕様本文に `対策 N` 番号を書かない**。
   - **単体テスト**を同ターンで追加・更新し、リポ方針どおり **`pytest`** で確認する。
4. **`local-docker-python-restart` SKILL** に従い、**`FishTrack`** で **`src/` 等アプリ用 Python** を変えたら **`docker compose restart app`** を**試行**する（検証・ユーザー報告の前）。
5. **正本レポート更新（実装と同一セッション）**  
   - **`## 6.`** の **リスト末尾**に **時系列昇順**で **実装記録**を追記する（**先頭タイムスタンプ `YYYY-MM-DD HH:mm:ss`（JST）・秒まで**。プレビュー未取得なら **未取得と明記**）。
   - **`## 4.`** から **反映完了した `対策 N`**（対応表・本文）を **削除**する。**「ソース反映済」等の完了宣言段落だけを §4 に足して対策本文を残すことは禁止**（**myrules**・**`ai-spec-check-report` SKILL**）。
   - **`## 5.`** に **削除した `対策 N` だけ**が載る項があれば **同タイミングで削除**し **(1)〜(n)** を振り直す。
   - **再プレビューが次の一手のみ**のときは **§4 に無根拠な `対策 M` を増やさない**（正本運用ルール「再プレビューだけ」の項）。
6. **markdownlint**  
   - 正本 `.md` を触れたら **`markdown-editing` / `markdownlint-fix` SKILL** に従い **0 件**まで揃える。
7. **CursorLog**  
   - **`obsidian-cursor-log` SKILL** に従い、ワークスペース変更があれば **`CursorLog/YYYY-MM/YYYY-MM-DD.md`** を更新する。

## 報告直前チェックリスト（本 SKILL 用）

**共通 SKILL §8.1「報告直前チェックリスト」** の項 1（`## 4.` を Read・残差削除）・項 2（`## 5.`・番号振り直し）も **正本を `Write` で確定する直前に**必ず適用する。実装ターンでは以下も **同一セッションで**済ませる。

- [ ] **`FishTrack`** で **`src/`・`tests/`** を変えた → **`docker compose restart app`** を試行した（または不可を 1 行で報告）。
- [ ] **§4 の当該 `対策 N`** を **削除**した（**反映済一行だけの追記で代替していない**。**対応表**に **`実装済`／`コード反映済`／履歴のみ** の行がないことも確認）。
- [ ] **`## 6.`（ソース反映ターン・プレビュー短行のみと混同しない）**を満たした：\
  **`FishTrack` `src`／`tests`** を **`ai_spec_check_report` §4 の打ち手**として変えた **同一セッション**では、**プレビュー未取得でも**リスト **末尾へ実装記録を 1 件以上**（先頭 **`YYYY-MM-DD HH:mm:ss`（JST）・秒まで**・未取得なら明示）。\
  **`## 1.` と同じ実行の `previewRunId` 短行**は、そのプレビューが §1 根のときのみ末尾へ。**二重しない**。**未到達／失敗**は **`requestId`／`jobId`** と**経路**。\
  **§4 対策に無関係な** `FishTrack` のみの変更のみのときは §6 は不要（正本運用）。
- [ ] **`ai_spec_check_report` をユーザー返答前に Read**：本条と §8.1 項 1 で残差・表と本文の不整合がないこと。**コードだけで締めることは共通 SKILL §8.1 と本条に反する**。
- [ ] **`## 5.`** を **§4 消込と噛み合わせて**整理した。
- [ ] **`CursorLog`** を更新した（対象があれば）。

## 明示的にやらないこと

- **§4 に §2〜§3 の観測だけを転記**して対策本文を置き換えること（観測は §1〜§3・§6）。詳細は **`ai-spec-check-report` SKILL**。
- **プレビュー実行・dump・本番ログ取得**が必要なときは **`ai-spec-check-virtual`** / **`ai-spec-check-local`** / **`ai-spec-check`** の **各入口 SKILL** を使い、終了後 **`ai-spec-check-report`** を Read する。
