# git-workflow

Always respond in Japanese when applying this skill.

## 発火条件

- **`git commit` / `git push` / PR・マージ**の実行可否を判断するとき
- ブランチ方針・無断コミット禁止・LF 改行の確認時
- 手順の正本は **`SKILL.md`**

## 製品別

- FishTrack / MyPokedex / otayori-navi の**具体手順**は各リポの `*_commit` / `*_pull-request` SKILL を Read
