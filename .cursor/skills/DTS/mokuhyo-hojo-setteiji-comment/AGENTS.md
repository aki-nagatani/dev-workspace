# mokuhyo-hojo-setteiji-comment

**最新期**の **`設定時コメント(補助・１次調整者)`** を、**長谷　晃英以外**の全メンバー各枝番について生成・反映する SKILL。手順の正本は **`SKILL.md`**。

- **更新項目**: **`##### 設定時コメント(補助・１次調整者)`** のみ
- **除外**: **`### … 長谷　晃英`** 配下すべて
- **参考・ベースライン**: **対象期の既存コメント**（あれば最優先）＋ **対象期より前**の同項目（**同一メンバーに限らない**。**枝番番号は突合に使わない**。論点は **同一目標ジャンル**優先、**文体・構成は直近**優先）。過去コメントは**論点・構成の土台として使ってよい**
- **既存あり**: **ベースラインを踏まえ対象期に合わせ再生成して差し替え**（無変更複写・上書き省略は禁止。ユーザーが「触らない」と明示した枝番のみ例外）
- **最新 SKILL**: 作業開始時に **`SKILL.md` 全文 Read**。既存文は以前の SKILL で書かれうるため、**実行時点の SKILL を正**に再生成（既存の形式に合わせない）
- **署名**: コメントブロック**最終行**は必ず **`（補助・長谷）`**（旧 **`（長谷）`** 禁止）
- **字数**: **`（補助・長谷）` 含む全体**で**おおむね100文字程度**（**SKILL**「書き方」。**1段落**基本）
- **執筆者**: 長谷は **2021 年に PL 就任**。**社内呼称はプロジェクトリーダー（PL）**（PM と同義）。コメントは **PL としての立場・経験・知識**を意識（**SKILL**「PL・PM としてのコメント」）
- **トーン**: **課題改善だけ**に偏らず、**成長・ポジションアップ・担える役割**の**ポジティブ要素を必ず**含める（**SKILL**「トーンのバランス」）
- **AI 痕跡**: **54 期長谷**の文体に寄せ、定型・抽象ラベル連打を避ける（**SKILL**「長谷文体・AI 痕跡の排除」）
- **アドバイス**: **必須ではない**。内容に応じて適宜（**SKILL**「アドバイス要素」）
- **本人記載の繰り返し禁止**: 通期目標・設定理由の**言い換えだけ**は書かない（**SKILL**「本人記載の繰り返し禁止」）
- **`課題.md`**: **Read 推奨**（組織・PL 文脈の参考。**編集は `work-kadai-update`**。本文・打ち手の復唱は禁止）
- **本人 12 項目**は **`mokuhyo-draft`**、校閲は **`mokuhyo-proofread`**
- **CSV 同期**: `.md` 更新後は同一セッションで **`人事考課_管理シート.csv`** を必ず更新（**`mokuhyo-kanri-sheet-csv`** SKILL）
- 完了後 **`obsidian-cursor-log`**。Markdown 編集後 **`markdownlint-fix`**

Always respond in Japanese when using this skill.
