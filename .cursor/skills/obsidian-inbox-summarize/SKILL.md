---
name: obsidian-inbox-summarize
description: MarkdownファイルまたはPDFファイルからKnowledge（知識・知見）を抽出してナレッジとして保存する。ファイルの内容を分析し、記事から得られる実用的な知識を抽出して適切な場所にノートを作成し、タグやリンクを付与する。InBoxのノートを処理したい時や、直接ファイルを指定して処理したい時に使用する。処理後は必ず「プロジェクトへの適用可能性の検討と提案」を実行する。
---

# Obsidian InBox Summarize

## 🚨 処理完了時の必須チェック（忘れずに確認）

処理完了前に、以下を必ず実行したか確認すること：

- [ ] **プロジェクトへの適用可能性の検討と提案**（必須・省略禁止）
  - 処理した内容が FishTrack、MyPokedex、otayori-navi、personal-tools、dev-workspace に適用できる場合、または検討する価値がある場合、報告に「## プロジェクトへの適用可能性」セクションを追加して提案を提示
  - 該当が無い場合は「該当なし」と明記する
- [ ] **Cursorログの更新**

## 適用条件
- 対象は以下のいずれか：
  - InBoxディレクトリ配下のMarkdownファイルおよびPDFファイル
  - ユーザーが直接指定したMarkdownファイルまたはPDFファイル（Cursorから直接渡される場合も含む）
- コマンドファイルで指定されたInBoxディレクトリとKnowledge保存先ディレクトリを使用する
- 直接ファイルが指定された場合は、そのファイルを処理対象とする

## 処理フロー

### 1. ノートの読み込み
- **入力ソースの確認**:
  - ユーザーが直接ファイルを指定した場合: そのファイルを処理対象とする
  - ファイルが指定されていない場合: InBoxディレクトリ配下のMarkdownファイルおよびPDFファイルを確認し、1件を選択
- 各ノートの内容を読み込む
  - **Markdownファイルの場合**: ファイルの内容を直接読み込む
  - **PDFファイルの場合**: PDFからテキストを抽出して読み込む。`dev-workspace/scripts/extract_pdf_text.py` を使用する（pypdf 失敗時は おたよりナビ OCR API → Azure Document Intelligence の順でフォールバック）。または Pythonの`pypdf`または`pdfplumber`ライブラリを直接使用する。
- **重要**: 1つのノートに複数の項目が含まれている場合、1つの項目だけを処理対象とする

**入力形式**:
InBoxノートには以下の4種類の形式がある：

1. **URL形式**: ノートの内容がURLのみの場合
   - 例: `https://qiita.com/degudegu2510/items/eff0f63dac99554e6439`
   - URLの内容を取得して分析する（可能な場合）
   - **URLの内容を取得できない場合（タイムアウト、エラー、アクセス不可等）**: 処理を中断する
   - **複数URLがある場合**: 1つのURLだけを処理対象とする（先頭のURL、または最初に見つかったURL）

2. **WEBクリップ形式**: ObsidianのWeb Clipperで保存された形式
   - フロントマターにURLやメタデータが含まれる
   - 本文にWebページの内容がMarkdown形式で保存されている
   - 例:
     ```markdown
     ---
     url: https://example.com/article
     title: 記事タイトル
     ---
     
     # 記事タイトル
     
     記事の本文内容...
     ```
   - **複数のWEBクリップがある場合**: 1つのクリップだけを処理対象とする（先頭のセクション）

3. **PDF形式**: PDFファイルが指定された場合
   - PDFファイルがInBoxディレクトリ配下に配置されている、またはユーザーが直接指定したPDFファイル
   - PDFからテキストを抽出して分析する
   - **PDFの読み込み方法**: `dev-workspace/scripts/extract_pdf_text.py` を使用する（推奨）。このスクリプトは (1) pypdf でテキスト抽出を試行し、(2) テキストが十分得られない場合におたよりナビ OCR API（`http://localhost:5003/` 固定）でフォールバック、(3) それでも失敗時は Azure Document Intelligence でフォールバックする。
   - **代替**: Pythonの`pypdf`または`pdfplumber`ライブラリを直接使用してPDFからテキストを抽出することも可能。
   - **おたよりナビ OCR API フォールバック**: ローカル Docker で otayori-navi が `http://localhost:5003` で稼働している場合、pypdf 失敗時に検索可能PDF API を呼び出して再抽出する。URLは固定のため環境変数不要。認証は不要（`OTAYORI_OCR_API_KEY` は任意）。
   - **Azure Document Intelligence フォールバック**: 上記が失敗または未設定の場合に使用。環境変数 `AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT` と `AZURE_DOCUMENT_INTELLIGENCE_KEY` が設定されているとき、かつ `azure-ai-documentintelligence` がインストールされているときのみ有効。
   - **PDFの読み込みに失敗した場合（ファイル破損、暗号化、アクセス不可等）**: 処理を中断する
   - **複数PDFがある場合**: 1つのPDFだけを処理対象とする（先頭のPDF、または最初に見つかったPDF）
   - **PDFファイルの処理後**: テキスト抽出が完了したら、PDFファイルを参考資料としてObsidian内に保存する
     - Knowledgeノートと同じディレクトリに`attachments`サブディレクトリを作成（存在しない場合）
     - PDFファイルを`attachments`ディレクトリにコピーして保存
     - ノートの「参照」セクションにPDFファイルへのリンクを追加（例: `[[attachments/ファイル名.pdf]]`）
     - InBoxディレクトリ内のPDFファイルは削除する（直接指定されたPDFファイルは削除しない）

4. **手入力メモ形式**: ユーザーが手動で入力したメモ
   - フロントマターがない、または最小限のフロントマターのみ
   - 本文にユーザーが記述した内容が含まれる
   - 例:
     ```markdown
     Cursorのショートカットキーについて
     
     - Cmd+K: インライン編集
     - Cmd+L: チャット開始
     ```
   - **複数のメモ項目がある場合**: 1つの項目だけを処理対象とする（先頭のセクション、または明確に区切られた最初の項目）

### 2. 内容分析とKnowledge抽出
- ノートの内容を分析し、記事から得られるKnowledge（知識・知見・実用的な情報）を抽出
- **入力形式に応じた処理**:
  - **URL形式の場合**: 
    - URLの内容を取得して分析（可能な場合）
    - **重要**: URLの内容を取得できない場合（タイムアウト、エラー、アクセス不可等）は、処理を中断する
    - URL取得に失敗した場合は、InBoxノートを削除せず、エラーメッセージと共に処理中断の旨を報告する
  - **WEBクリップ形式の場合**: フロントマターからURLやタイトルを抽出し、本文内容を分析（本文が存在する場合は本文を分析）
  - **PDF形式の場合**: 
    - PDFファイルからテキストを抽出して分析
    - **PDFの読み込み方法**: まず `dev-workspace/scripts/extract_pdf_text.py <PDFパス>` を実行してテキストを取得する。このスクリプトは pypdf → おたよりナビ OCR API（`http://localhost:5003` 固定、otayori-navi 稼働時）→ Azure Document Intelligence（テキスト不足時）の順で試行する。スクリプトが使えない場合は、Pythonの`pypdf`または`pdfplumber`を直接使用する。
    - **PDFの読み込みに失敗した場合（ファイル破損、暗号化、アクセス不可等）**: 処理を中断する
    - PDFファイルを削除せず、エラーメッセージと共に処理中断の旨を報告する
    - テキスト抽出が成功したら、抽出したテキストを分析してKnowledgeを抽出
    - **PDFファイルの保存**: テキスト抽出が完了したら、PDFファイルを参考資料としてObsidian内に保存する
      - Knowledgeノートと同じディレクトリに`attachments`サブディレクトリを作成（存在しない場合）
      - PDFファイルを`attachments`ディレクトリにコピーして保存
      - ノートの「参照」セクションにPDFファイルへのリンクを追加（例: `[[attachments/ファイル名.pdf]]`）
  - **手入力メモ形式の場合**: 本文内容を直接分析（フロントマターがあればメタデータも抽出）
- **ジャンルを特定**（例: Cursor、Python、AWS、Raspberry Pi など）
- **Knowledge（知識）を抽出・整理**:
  - 記事の要約ではなく、記事から得られる実用的な知識・知見・ノウハウを抽出
  - 具体的な手順、ベストプラクティス、注意点、使用例、テンプレート、設定方法など、実際に活用できる情報を整理
  - 単なる記事の要約ではなく、自分が今後参照・活用するための知識として構造化
- 元の内容への参照を保持（URL、引用など。手入力メモの場合は参照セクションは省略可）
- キーワードやタグ候補を抽出（同ジャンルノート検索に使用）

**ジャンルの特定方法**:
- 固有名詞（ツール名、サービス名、技術名など）を抽出
- 主要トピックを特定
- 例: 「Cursor」に関する記事 → ジャンル: `Cursor`
- 例: 「Pythonのデコレータ」に関する記事 → ジャンル: `Python`
- 例: 「AWS Lambda」に関する記事 → ジャンル: `AWS`

### 3. 既存ノートの検索
Knowledge保存先ディレクトリ配下の既存ノートを検索し、同ジャンルのノートを探す。

**同ジャンルの判定基準**（優先順位順）:
1. **タグの一致**: 抽出したタグ候補と既存ノートのタグが一致または類似
2. **キーワードの一致**: 主要キーワードが既存ノートのタイトルや内容に含まれる
3. **タイトルの類似性**: タイトルが同じトピックを扱っている
4. **内容の関連性**: 内容が同じ分野・トピックを扱っている

**検索方法**:
- Knowledge保存先ディレクトリ配下の全Markdownファイルを確認
- 各ファイルのタイトル、タグ、主要キーワードを抽出
- InBoxノートの内容と比較して関連性を判定

### 4. ノートの作成または更新

#### 既存ノートが見つかった場合（追記・更新）
**重要**: 既存ノートの内容を読み込み、新しい情報を既存の文章構造に統合する形で追記する。

**追記方法**:
1. **既存ノートの内容を完全に読み込む**
   - 既存ノートの全体構造を把握
   - 既存のセクション、見出し、内容を理解
   - 既存の文章スタイルや記述方法を確認

2. **既存のセクションに統合**
   - **既存のセクションが存在する場合**: そのセクションに新しい情報を自然に追加
     - 例: 「使い方」セクションが既にある場合、新しい使い方の情報をそのセクションに追加
     - 例: 「特徴」セクションが既にある場合、新しい特徴をそのセクションに追加
   - **既存のセクションがない場合**: 適切な場所に新しいセクションを追加
     - 既存の構造に合わせて、適切な見出しレベルで追加
     - 既存のセクションの順序や階層を尊重

3. **参照セクションの更新**
   - 既存の参照セクションがあれば、そこに新しい参照を追加
   - 既存の参照セクションがない場合は、適切な場所に参照セクションを追加

4. **フロントマターの更新**
   - `updated`フィールドがあれば、現在の日付に更新
   - `updated`フィールドがない場合は追加

**追記時の注意事項**:
- **既存の文章構造を維持**: 既存の見出し階層、セクション構成を壊さない
- **既存の文章スタイルに合わせる**: 既存の記述方法や文体に合わせて新しい情報を追加
- **重複を避ける**: 既存の内容と重複する情報は追加しない（必要に応じて既存の内容を更新）
- **矛盾がある場合**: 既存ノートの内容と新しい情報に矛盾がある場合（例: 既存ノートに「AはBである」と記載されているが、新しい情報に「AはCである」と記載されている場合）は、自動的に統合せず、ユーザーにどちらを採用するか問いかけること
  - 矛盾の内容を明確に示す（既存の内容と新しい情報の両方を提示）
  - ユーザーの判断を待ってから処理を続行する
- **自然な統合**: 新しい情報が既存の文章に自然に統合されるようにする
- **既存のタグは維持**: 既存のタグは維持し、必要に応じて新しいタグを追加
- **既存のリンクを尊重**: 既存のWikiリンクやMarkdownリンクを維持

#### 既存ノートが見つからなかった場合（新規作成）

**ファイル名**:
- **ジャンル名をファイル名にする**（例: `Cursor.md`、`Python.md`、`AWS.md`）
- ジャンルが特定できない場合は、主要キーワードから適切な名前を決定
- 既存ファイルと重複しないようにする（重複する場合は、ジャンル名に番号や日付を追加しない。既存ノートに追記する）

**ノート構造**:
```markdown
---
created: YYYY-MM-DD
source: InBox/YYYY-MM-DD.md
---

# [ノートタイトル]

## Knowledge
[記事から得られる知識・知見・実用的な情報]
- 具体的な手順、ベストプラクティス、注意点、使用例、テンプレート、設定方法など
- 単なる記事の要約ではなく、実際に活用できる知識として整理

## 詳細
[必要に応じて詳細情報]

## 参照
[元の内容への参照、URLなど。PDFファイルから解析した場合は、PDFファイルへのWikiリンクも含める（例: `[[attachments/ファイル名.pdf]]`）]

## 関連リンク
[関連するノートへのWikiリンク]
```

### 5. タグとリンクの付与
`obsidian-update-rules` のルールに従って：

- 重要語（固有名詞、システム名、機器名、手順名、プロジェクト名）が出たら、可能なら `[[既存ノート名]]` へのWikiリンクを優先
- 該当ノートが存在しない場合は、タグ `#keyword` を付与（英小文字・ハイフン推奨）
- 既存タグ体系がある場合はそれに合わせる（例: `#infra/raspi` のような階層タグ）
- 1ノート内のタグは過剰にならない範囲で付与（目安: 3〜8個）

### 6. 元のノートの処理
- **InBoxディレクトリ内のファイルの場合**: 
  - 要約ノート作成・更新が完了したら、処理した項目をInBoxノートから削除
  - **重要**: 1つのノートに複数の項目がある場合、処理した項目だけを削除し、残りの項目はノートに残す
  - ノート内のすべての項目を処理した場合のみ、ノートファイル全体を削除する
  - 削除前に要約ノートへの参照を確認
- **直接指定されたファイルの場合**: 
  - ファイルは削除しない（ユーザーが明示的に指定したファイルのため）
- **重要**: 1件処理したら作業を中断する（次のノートまたは次の項目は別の実行で処理）

## 実行手順

1. **入力ソースの確認**
   - **ユーザーが直接ファイルを指定した場合**: そのファイルを処理対象とする（InBoxディレクトリの確認は不要）
   - **ファイルが指定されていない場合**: 
     - InBoxディレクトリ配下のMarkdownファイルおよびPDFファイルの一覧を取得
     - 処理対象のノート数を確認

2. **1件だけ処理する**（この実行では1件のみ）
   
   処理対象のノートを1つ選び（直接指定された場合はそのファイル、指定されていない場合はInBoxから未処理のノートを1つ選択）、以下を実行：
   
   a. **ノートの読み込み**
      - 1件のノートファイル（MarkdownまたはPDF）を読み込む
      - 直接指定された場合はそのファイル、指定されていない場合は先頭のノート、または日付が古い順など、1件に限定
      - **Markdownファイルの場合**: ファイルの内容を直接読み込む
      - **PDFファイルの場合**: `python dev-workspace/scripts/extract_pdf_text.py <PDFパス>` を実行してテキストを抽出（pypdf 失敗時は おたよりナビ OCR API（localhost:5003 固定）→ Azure Document Intelligence でフォールバック。otayori-navi 稼働時は API を利用可）。または Pythonの`pypdf`または`pdfplumber`ライブラリを直接使用する。
      - ノート内に複数の項目があるか確認（Markdownファイルの場合）
      - 複数の項目がある場合、1つの項目だけを処理対象とする（先頭の項目、または最初に見つかった項目）
   
   b. **内容分析**
      - 処理対象の項目の内容を分析し、ジャンル、Knowledge（知識）、キーワードを抽出
      - 入力形式を判定（URL形式、WEBクリップ形式、PDF形式、手入力メモ形式）
      - **URL形式の場合**: 
        - URLの内容を取得して分析を試みる
        - **URLの内容を取得できない場合（タイムアウト、エラー、アクセス不可等）**: 処理を中断する
        - InBoxノートを削除せず、エラーメッセージと共に処理中断の旨を報告する
        - 次のステップ（既存ノートの検索、ノートの作成または更新等）には進まない
      - **PDF形式の場合**: 
        - PDFファイルからテキストを抽出する
        - `extract_pdf_text.py` を実行するか、Pythonの`pypdf`または`pdfplumber`を使用してPDFを読み込み、テキストを抽出（推奨: extract_pdf_text.py は pypdf 失敗時に おたよりナビ OCR API（localhost:5003 固定）→ Azure Document Intelligence でフォールバック。otayori-navi 稼働時は API 利用可）
        - **PDFの読み込みに失敗した場合（ファイル破損、暗号化、アクセス不可等）**: 処理を中断する
        - PDFファイルを削除せず、エラーメッセージと共に処理中断の旨を報告する
        - 次のステップ（既存ノートの検索、ノートの作成または更新等）には進まない
        - テキスト抽出が成功したら、抽出したテキストを分析してKnowledgeを抽出
        - **PDFファイルの保存**: テキスト抽出が完了したら、PDFファイルを参考資料としてObsidian内に保存する
          - Knowledgeノートと同じディレクトリに`attachments`サブディレクトリを作成（存在しない場合）
          - PDFファイルを`attachments`ディレクトリにコピーして保存
          - ノートの「参照」セクションにPDFファイルへのリンクを追加（例: `[[attachments/ファイル名.pdf]]`）
   
   c. **既存ノートの検索**
      - ジャンル名で既存ノートを検索（例: ジャンルが「Cursor」の場合、Knowledge保存先ディレクトリ内の`Cursor.md`を検索）
      - Knowledge保存先ディレクトリ配下で既存の同ジャンルノートを検索
   
   d. **ノートの作成または更新**
      - **既存ノートが見つかった場合**: 
        1. 既存ノートの内容を完全に読み込む
        2. 既存の文章構造を理解し、新しい情報と既存の内容を比較
        3. **矛盾がある場合**: 既存ノートの内容と新しい情報に矛盾がある場合は、自動的に統合せず、ユーザーにどちらを採用するか問いかける
           - 矛盾の内容を明確に示す（既存の内容と新しい情報の両方を提示）
           - ユーザーの判断を待ってから処理を続行する
        4. 既存の文章構造を理解し、新しい情報を既存のセクションに統合する形で追記
        5. 既存のセクションがない場合は、適切な場所に新しいセクションを追加
        6. 既存の参照セクションに新しい参照を追加、または参照セクションを作成
        7. フロントマターの`updated`フィールドを現在の日付に更新
      - **既存ノートが見つからなかった場合**: ジャンル名をファイル名として新規ノートを作成（Knowledge保存先ディレクトリ内に作成）
   
   e. **タグとリンクの付与**
      - タグとリンクを付与（既存ノートの場合は既存タグを維持）
   
   f. **元のノートの処理**
      - **InBoxディレクトリ内のファイルの場合**: 
        - **処理が完了したら、処理した項目をInBoxノートから削除**
        - **Markdownファイルの場合**: 
          - **1つのノートに複数の項目がある場合**: 処理した項目だけを削除し、残りの項目はノートに残す
          - **ノート内のすべての項目を処理した場合**: ノートファイル全体を削除する
        - **PDFファイルの場合**: 
          - テキスト抽出とKnowledge抽出が完了し、PDFファイルを参考資料として`attachments`ディレクトリに保存したら、InBoxディレクトリ内のPDFファイルを削除する
          - 削除前に要約ノートへの参照を確認
        - 削除前に要約ノートへの参照を確認
      - **直接指定されたファイルの場合**: 
        - ファイルは削除しない（ユーザーが明示的に指定したファイルのため）
        - 処理完了後、ファイルの場所を報告する
   
   g. **処理結果の報告**
      - 処理したノートのファイル名（MarkdownファイルまたはPDFファイル）
      - 入力ソース（InBoxディレクトリ内のファイル、または直接指定されたファイル）
      - 処理した項目の内容（URL、メモの内容、PDFの内容など）
      - 抽出したKnowledge（知識）の概要
      - 新規作成したノートの場所、または更新した既存ノートの場所
      - 付与したタグやリンク
      - ジャンル名
      - ノートに残りの項目がある場合は、その旨を報告（Markdownファイルの場合）
      - 直接指定されたファイルの場合は、ファイルが削除されていないことを報告
      - **🚨 プロジェクトへの適用可能性の検討と提案（必須）**: 下記 h. を実行し、報告の最後に「## プロジェクトへの適用可能性」を必ず含める
   
   h. **🚨 プロジェクトへの適用可能性の検討と提案（必須・省略禁止）**
      - **忘れやすいので必ず実行する**。処理した内容が現在のプロジェクト（FishTrack、MyPokedex、otayori-navi、personal-tools、dev-workspace）に適用できる場合、または検討する価値がある場合、報告に提案を追加する
      - **該当が無い場合も「該当なし」と明記する**
      - **検討対象**:
        1. **技術スタックの一致**: 処理した内容が使用している技術（Docker、AWS、Python、PostgreSQLなど）と一致する場合
        2. **アーキテクチャの改善**: 現在の構成を改善できる可能性がある場合（例: EC2上のDocker ComposeからECSへの移行、マイグレーション実行方法の改善など）
        3. **ベストプラクティスの適用**: 記事で紹介されているベストプラクティスが現在の実装に適用できる場合
        4. **開発環境の改善**: 開発環境の効率化や改善に役立つ知識がある場合
        5. **本番環境の最適化**: 本番環境の運用や構成の最適化に役立つ知識がある場合
      - **提案方法**:
        - 処理結果の報告の最後に「## プロジェクトへの適用可能性」セクションを追加
        - 適用可能な内容を具体的に示し、現在の実装との比較や改善点を提示
        - 検討すべき点や実装時の注意点を記載
        - 提案は簡潔に、実用的な内容に絞る（長文の分析は避ける）
      - **提案の例**:
        - 「現在のプロジェクトはEC2上でDocker Composeを使用していますが、記事で紹介されているECSへの移行を検討することで、スケーラビリティや運用の自動化が向上する可能性があります」
        - 「マイグレーションの実行方法について、記事で紹介されている一時的なECSタスクとして実行する方法を検討することで、APIコンテナを停止せずにマイグレーションを実行できる可能性があります」
        - 「開発環境でのDockerの使い方は既に実装されていますが、記事で紹介されているDockerイメージとコンテナの関係の理解を深めることで、本番環境の構成をより明確に整理できる可能性があります」
   
   i. **Cursorログの更新（必須）**
      - **処理完了後、必ずCursorログを更新する**
      - `obsidian-cursor-log` SKILLを使用して、当日のCursorLogに作業内容を記録する
      - 記録内容は処理結果の報告（プロジェクトへの適用可能性を含む）を基に、適切な形式で記録する

3. **Cursorログの更新（必須）**
   - **処理完了後、必ずCursorログを更新する**
   - `obsidian-cursor-log` SKILLを使用して、当日のCursorLog（`D:/OneDrive/アプリ/remotely-save/Obsidian/CursorLog/YYYY-MM-DD.md`）に作業内容を記録する
   - **記録内容**:
     - 作業名: InBoxノート処理（処理したノートのファイル名、ジャンル名を含める）
     - プロジェクト名: dev-workspace、Obsidian
     - 変更ファイル: 処理したファイル数と主要なファイル名（InBoxノート、Knowledgeノート、SKILLファイルなど）
     - 実施内容: 処理したノートの内容、抽出したKnowledgeの概要、プロジェクトへの適用可能性の提案（該当する場合）
     - 結果: 新規作成または更新したノートの場所、付与したタグやリンク、InBoxノートの残り項目数など
   - **タグ**: 作業内容に応じて適宜追加（例: `#obsidian`、`#inbox-summarize`、処理したジャンルのタグなど）
   - **重要**: Cursorログの更新を忘れないこと。これは必須の作業である。

4. **作業の中断**
   - **1件処理したら、ここで作業を中断する**
   - InBoxに未処理のノートが残っていても、続けて処理しない
   - 次のノートは、このコマンドを再実行して処理する

## 注意事項

- 既存のMarkdown構造・見出し階層を維持
- 既存のリンク形式（Wikiリンク `[[...]]` / Markdownリンク `[text](url)`）を尊重
- ノートの文脈を壊さない最小限の編集で完結
- ファイルの改行は既存に合わせる（不用意に整形しない）
- 既存の関連リンクがあるか確認し、重複追加しない
- 既存タグを再利用し、必要に応じて最小限の追加
- **既存ノートに追記する場合**: 単純にKnowledgeの内容を追記するのではなく、既存のノートの内容を読み込み、その文章にマージするように追記すること
  - 既存のセクションが存在する場合は、そのセクションに新しい知識を自然に追加
  - 既存のセクションがない場合は、適切な場所に新しいセクションを追加
  - 既存の文章構造・スタイルを維持し、新しい知識を自然に統合
  - 既存の内容と重複する情報は追加しない（必要に応じて既存の内容を更新）
  - **矛盾がある場合**: 既存ノートの内容と新しい情報に矛盾がある場合は、自動的に統合せず、ユーザーにどちらを採用するか問いかけること。矛盾の内容を明確に示し、ユーザーの判断を待ってから処理を続行する
  - **既存ノートに「要約」セクションがある場合**: 「Knowledge」セクションに統合するか、既存の「要約」セクションを「Knowledge」に変更することを検討
- **URLの取得に失敗した場合**: URL形式のノートでURLの内容を取得できない場合（タイムアウト、エラー、アクセス不可等）は、処理を中断する
  - InBoxノートを削除しない
  - エラーメッセージと共に処理中断の旨を報告する
  - 次のステップ（既存ノートの検索、ノートの作成または更新等）には進まない
- **PDFの読み込みに失敗した場合**: PDF形式のファイルでPDFの読み込みに失敗した場合（ファイル破損、暗号化、アクセス不可等）は、処理を中断する
  - **extract_pdf_text.py 使用時**: pypdf でテキストが得られず、かつ おたよりナビ OCR API・Azure Document Intelligence のフォールバックも失敗または未設定の場合は処理を中断する
  - PDFファイルを削除しない
  - エラーメッセージと共に処理中断の旨を報告する
  - 次のステップ（既存ノートの検索、ノートの作成または更新等）には進まない
- **PDFのテキスト抽出**: PDFからテキストを抽出する際は、可能な限り正確にテキストを抽出する
  - 表や図が含まれるPDFの場合は、`pdfplumber`ライブラリの使用を推奨（より高度なレイアウト解析が可能）
  - テキスト抽出が不完全な場合でも、抽出できた部分からKnowledgeを抽出する
- **PDFファイルの保存**: PDFファイルから解析した場合は、PDFファイルを参考資料としてObsidian内に保存する
  - Knowledgeノートと同じディレクトリに`attachments`サブディレクトリを作成（存在しない場合）
  - PDFファイルを`attachments`ディレクトリにコピーして保存
  - ノートの「参照」セクションにPDFファイルへのWikiリンクを追加（例: `[[attachments/ファイル名.pdf]]`）
  - InBoxディレクトリ内のPDFファイルは、参考資料として保存した後に削除する
  - 直接指定されたPDFファイルは削除しない（ユーザーが明示的に指定したファイルのため）
- **🚨 プロジェクトへの適用可能性の検討と提案（必須・省略禁止）**: 毎回必ず実行する。忘れやすいが省略してはならない
  - 処理した内容が現在のプロジェクトに適用できる場合や検討する価値がある場合は、提案を提示する
  - **該当が無い場合も「該当なし」と明記する**
  - 提案は処理結果の報告の最後に「## プロジェクトへの適用可能性」セクションとして追加
  - 提案は簡潔に、実用的な内容に絞る（長文の分析は避ける）
  - 現在の実装との比較や改善点を具体的に示す
  - 検討すべき点や実装時の注意点を記載する
- **Cursorログの更新（必須）**: 処理完了後、必ずCursorログを更新する
  - `obsidian-cursor-log` SKILLを使用して、当日のCursorLogに作業内容を記録する
  - 記録内容は処理結果の報告の内容を基に、適切な形式で記録する
  - Cursorログの更新を忘れないこと。これは必須の作業である。

## 例

### 例1: URL形式（既存ノートへの追記）

**入力**: InBoxに `2026-01-27.md` があり、内容が `https://azukiazusa.dev/blog/new-design-tool-pencil`

**既存ノート**: Knowledge保存先ディレクトリ内の`Pencil.md`が既に存在し、以下のような構造になっている：
```markdown
# Pencil

## Knowledge
Pencilは、AIがUIデザインを生成できるデザインツール...

## 使い方
### Pencil(アプリ) x Claude Code(CLI)
1. 公式サイトからアプリをインストール
...

## 参照
- [Pencil公式サイト](https://www.pencil.dev/)
```

**処理**:
1. URL形式であることを認識
2. URLの内容を取得して分析
3. 内容を分析してジャンル、Knowledge（知識）、キーワードを抽出
   - ジャンル: 「Pencil」
   - Knowledge: インストール手順、プロンプトでのデザイン生成方法、MCPでのコード書き出し手順など、実際に活用できる知識
4. 既存ノート（Knowledge保存先ディレクトリ内の`Pencil.md`）を発見
5. **既存ノートの内容を完全に読み込む**
6. **既存の文章構造に統合する形で追記**:
   - 「使い方」セクションが既にあるため、新しい使い方の知識（プロンプトでのデザイン生成、MCPでのコード書き出し）をそのセクションに追加
   - 「インストールとセットアップ」という新しいサブセクションを「使い方」セクション内に追加（既存の構造に合わせて）
   - 参照セクションに新しいURLを追加
7. フロントマターの`updated`フィールドを`2026-01-27`に更新
8. タグやリンクを付与（既存のタグは維持）
9. 処理した項目をInBoxノートから削除
10. **プロジェクトへの適用可能性の検討**:
    - 処理した内容が現在のプロジェクトに適用できる場合、提案を提示
    - 例: 「PencilはUIデザイン生成ツールとして、現在のプロジェクトのフロントエンド開発に活用できる可能性があります。特に、プロトタイプ作成やデザインの迅速な検証に役立つ可能性があります」

**追記後の例**:
```markdown
## 使い方

### インストールとセットアップ
- MacOSとLinuxのみ対応（現時点）
- メールアドレス登録と確認コードでのアクティベーションが必要
...

### Pencil(アプリ) x Claude Code(CLI)
[既存の内容を維持]

### プロンプトでデザインを生成
- キャンバス左下の「Design with Claude Code」チャット欄からデザイン指示を入力
- 例: 「Create a login form with email and password fields, and a submit button.」
...

### Pencil MCPでコードを書き出す
- Pencilインストール時にClaude Codeに自動でPencil MCPが追加される
...
```

### 例2: WEBクリップ形式（既存ノートへの追記）

**入力**: InBoxに `2026-01-26.md` があり、内容が：
```markdown
---
url: https://example.com/python-decorator
title: Pythonデコレータの使い方
---

# Pythonデコレータの使い方

デコレータは関数を拡張するための機能です...
```

**既存ノート**: `Notes/Knowledge/Python.md` が既に存在し、以下のような構造になっている：
```markdown
# Python

## Knowledge
Pythonは高水準プログラミング言語...

## 基本的な構文
[既存の内容]
```

**処理**:
1. WEBクリップ形式であることを認識
2. フロントマターからURLとタイトルを抽出
3. 本文内容を分析してジャンル、Knowledge（知識）、キーワードを抽出
   - ジャンル: 「Python」
   - Knowledge: デコレータの使い方、実用的なパターン、ベストプラクティスなど
4. 既存ノート（Knowledge保存先ディレクトリ内の`Python.md`）を発見
5. **既存ノートの内容を完全に読み込む**
6. **既存の文章構造に統合する形で追記**:
   - 「基本的な構文」セクションの後に「デコレータ」という新しいセクションを追加
   - または、既存の「高度な機能」セクションがあれば、そこにデコレータの知識を追加
   - 参照セクションに新しいURLを追加
7. フロントマターの`updated`フィールドを更新
8. タグやリンクを付与（既存のタグは維持）
9. 処理した項目をInBoxノートから削除

### 例3: 手入力メモ形式（既存ノートへの追記）

**入力**: InBoxに `2026-01-26.md` があり、内容が：
```markdown
Cursorのショートカットキーについて

- Cmd+K: インライン編集
- Cmd+L: チャット開始
- Cmd+I: インライン補完
```

**既存ノート**: `Notes/Knowledge/Cursor.md` が既に存在し、以下のような構造になっている：
```markdown
# Cursor

## Knowledge
CursorはAI駆動のコードエディタ...

## 使い方
[既存の使い方の内容]
```

**処理**:
1. 手入力メモ形式であることを認識（フロントマターなし、URLなし）
2. 本文内容を直接分析
3. 内容を分析してジャンル、Knowledge（知識）、キーワードを抽出
   - ジャンル: 「Cursor」
   - Knowledge: ショートカットキーの使い方、効率的な操作方法など
4. 既存ノート（Knowledge保存先ディレクトリ内の`Cursor.md`）を発見
5. **既存ノートの内容を完全に読み込む**
6. **既存の文章構造に統合する形で追記**:
   - 「使い方」セクションが既にあるため、そのセクション内に「ショートカットキー」というサブセクションを追加
   - または、既存の「ショートカットキー」セクションがあれば、そこに新しいショートカットキーの知識を追加
   - 参照セクションは省略（手入力メモのため）
7. フロントマターの`updated`フィールドを更新
8. タグやリンクを付与（既存のタグは維持）
9. 処理した項目をInBoxノートから削除

### 例4: PDF形式（新規ノート作成）

**入力**: InBoxに `Python入門ガイド.pdf` というPDFファイルが配置されている

**処理**:
1. PDF形式であることを認識（`.pdf`拡張子のファイル）
2. Pythonの`pypdf`または`pdfplumber`ライブラリを使用してPDFからテキストを抽出
   - 例: `pypdf`を使用する場合
     ```python
     from pypdf import PdfReader
     reader = PdfReader("Python入門ガイド.pdf")
     text = ""
     for page in reader.pages:
         text += page.extract_text()
     ```
   - 例: `pdfplumber`を使用する場合
     ```python
     import pdfplumber
     with pdfplumber.open("Python入門ガイド.pdf") as pdf:
         text = ""
         for page in pdf.pages:
             text += page.extract_text()
     ```
3. 抽出したテキストを分析してジャンル、Knowledge（知識）、キーワードを抽出
   - ジャンル: 「Python」
   - Knowledge: Pythonの基本的な構文、実用的なコード例、ベストプラクティスなど
4. 既存ノート（Knowledge保存先ディレクトリ内の`Python.md`）を検索
   - **既存ノートが見つかった場合**: 既存ノートに追記（例2と同様の処理）
   - **既存ノートが見つからなかった場合**: 新規ノートを作成
5. **新規ノート作成の場合**:
   - Knowledge保存先ディレクトリ内に`Python.md`を新規作成
   - 抽出したKnowledgeを構造化して記述
   - フロントマターに`created`フィールドと`source`フィールドを追加
   - タグやリンクを付与
6. **PDFファイルの保存**:
   - Knowledgeノートと同じディレクトリに`attachments`サブディレクトリを作成（存在しない場合）
   - PDFファイルを`attachments`ディレクトリにコピーして保存（例: `attachments/Python入門ガイド.pdf`）
   - ノートの「参照」セクションにPDFファイルへのWikiリンクを追加（例: `[[attachments/Python入門ガイド.pdf]]`）
7. InBoxディレクトリ内のPDFファイルを削除
8. 処理結果を報告
9. **プロジェクトへの適用可能性の検討**:
   - 処理した内容が現在のプロジェクトに適用できる場合、提案を提示
   - 例: 「Pythonの知識は、現在のプロジェクト（FishTrack、MyPokedex）で使用されているPythonのコード品質向上に活用できる可能性があります」

### 例5: プロジェクトへの適用可能性の提案例（Docker記事の場合）

**処理した内容**: Dockerで開発して、AWSで本番運用する方法についての記事

**プロジェクトへの適用可能性**:
```markdown
## プロジェクトへの適用可能性

### 現在の実装状況
- **開発環境**: Docker Composeを使用してローカルで開発（docker-compose.ymlで定義）
- **本番環境**: EC2インスタンス上でDocker Composeを使用（ECSではない）
- **データベース**: RDSを使用（本番環境）

### 検討すべき改善点

1. **ECSへの移行の検討**
   - 現在のプロジェクトはEC2上でDocker Composeを使用していますが、記事で紹介されているECSへの移行を検討することで、以下のメリットが期待できます：
     - スケーラビリティの向上（自動スケーリング）
     - 運用の自動化（デプロイメントの簡素化）
     - コンテナのライフサイクル管理の改善
   - **注意点**: ECSへの移行には、Terraformのインフラコードの更新やCI/CDパイプラインの修正が必要

2. **マイグレーション実行方法の改善**
   - 現在のマイグレーション実行方法を確認し、記事で紹介されている一時的なECSタスクとして実行する方法を検討することで、APIコンテナを停止せずにマイグレーションを実行できる可能性があります
   - **現在の方法**: EC2インスタンス上で直接マイグレーションコマンドを実行
   - **提案**: ECSタスクとしてマイグレーションを実行する方法への移行を検討

3. **Dockerイメージとコンテナの理解の整理**
   - 開発環境でのDockerの使い方は既に実装されていますが、記事で紹介されているDockerイメージとコンテナの関係の理解を深めることで、本番環境の構成をより明確に整理できる可能性があります
   - 特に、「イメージは静的、コンテナは動的」という概念を理解することで、ECRとECSの関係もより明確になります
```

このように、処理した内容が現在のプロジェクトに適用できる場合や検討する価値がある場合は、具体的な提案を提示します。
